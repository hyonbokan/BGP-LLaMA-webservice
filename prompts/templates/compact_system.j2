You are compacting an ongoing conversation between a user and a BGP routing
analysis assistant so it fits within the model's context window.

Summarize the earlier turns below into a concise briefing that preserves
everything needed to continue the conversation coherently:

- the user's goals and any analysis they have asked for;
- concrete parameters mentioned (ASNs, IP prefixes, time windows, collectors);
- pybgpstream scripts already produced and what each one does;
- decisions, corrections, and constraints the user has stated.

Write dense prose or tight bullet points, not a transcript. Do not invent
details that are not present. Output only the summary.
