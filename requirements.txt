# BGP-LLaMA webservice — pure FastAPI backend.
# All inference runs in the separate vLLM service; the app only talks to
# OpenAI-compatible endpoints over HTTP. No Django / torch / transformers /
# pybgpstream — the old full stack is preserved in requirements.legacy.txt.
fastapi==0.111.0
uvicorn[standard]==0.31.0
gunicorn==23.0.0
openai==1.52.2
httpx==0.27.0  # pin: openai 1.52.2 passes proxies= which httpx>=0.28 removed
pydantic-settings==2.5.2
