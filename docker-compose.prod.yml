# Production override. Use via `make up-prod`.
# NOTE: TLS termination (443 + Let's Encrypt) needs a cert-aware nginx config;
# the base nginx.conf is HTTP-only. Add the TLS server block + cert mounts here
# when deploying behind a public domain.
services:
  api:
    environment:
      ENV: production
