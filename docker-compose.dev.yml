# Development override. Use via `make up-dev`.
services:
  web:
    environment:
      - DJANGO_SETTINGS_MODULE=project_1.settings.development

  fastapi:
    environment:
      - ENV=development

  nginx:
    ports:
      - "80:80"
