# Dev leaves these empty so the browser talks to the Vite proxy (same origin).
# The proxy forwards /api -> :8001 (Django) and /agent -> :8002 (FastAPI).
# Set explicit URLs only if you run the frontend against a non-proxied backend.
VITE_API_URL=
VITE_AGENT_URL=
